//if

const isUserLoggedIn = false;
if (isUserLoggedIn) {
    // console.log(`user is logged into the webisite`);
    
} else {
    // console.log(`user is not logged into the website`);
    
}

// const balance = 1000;
// if (balance < 500) {
//     console.log(`less then 500`);
    
// } else if(balance < 800) {
//     console.log(`less then 800`);
    
// } else {
//     console.log(`balance is greater than 800`);
    
// }

const isUserLoggedInGoogle = false;
const isUserLoggedInEmail = true;
if (isUserLoggedInGoogle || isUserLoggedInEmail) {
    console.log(`user is logged into the website`);
    
}